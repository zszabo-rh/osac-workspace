Use the quick-fix skill to launch an unattended bug fix in the background: create a Jira bug, write the fix with tests, verify build/format/tests pass, commit, create a PR, and move the ticket to Code Review. For interactive, phase-by-phase bug investigation, use /bugfix instead.

$ARGUMENTS