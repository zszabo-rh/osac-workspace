# Create Pull Request

Use the create-pr skill to validate code, push to the push remote, and create a PR against the upstream repo with proper title format. Runs repo-specific checks (build, test, lint) before submitting.

$ARGUMENTS
