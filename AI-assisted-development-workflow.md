# AI-Assisted Development Workflow

This file is a stub. The canonical Feature → PRD → Design → Jira sync →
Implement → E2E sequence lives in
[osac-ai-skills](https://github.com/osac-project/osac-ai-skills#recommended-skill-sequence)
(local after `./bootstrap.sh`: `~/.osac-ai-skills/README.md` or
`.osac-ai-skills/README.md`, section **Recommended Skill Sequence**).

Until workspace cutover, still run `./bootstrap.sh` from this workspace root
before a new session. Standalone `osac/` clones use `tools/bootstrap.sh` there
instead — do not run both.
